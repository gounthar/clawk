# Build artifacts
/clawk
/bin/
machine/bin/
machine/smoke-alpine
machine/smoke-firecracker
*.exe

# Guest kernel build scratch + output (images/guest-kernel/build.sh)
images/guest-kernel/build/
images/guest-kernel/vmlinux
images/guest-kernel/vmlinux-*

# Local cruft
.DS_Store
/vmlinux-arm64-snd
third_party/misc
debug-logs/
*.panic
*.log

# Local Claude Code settings and skills (personal, not shared)
.claude/settings.local.json
.claude/skills/

# Session working state. Records lab host addresses, SR uuids and who owns
# which VM — none of which belongs in a public repo.
CONTEXT.md
.env

# Local assistant instructions. Same reasoning as CONTEXT.md: personal
# working notes, and untracked files are one `git add -A` away from a public
# repo.
CLAUDE.md

# Working notes: implementation plans, scratch write-ups. Kept out of the
# repo because they age into fiction the moment the code lands — the design
# rationale that matters belongs in the code and in docs/.
.plans/
